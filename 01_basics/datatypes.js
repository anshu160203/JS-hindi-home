// Datatypes

// "use strict" // treat all js code as newer version of js code

// ECMA Script >> standards of java script >> tc 39...... mdn(for js)

//  data types >> 
                    // number => 2 to power 53 >> integer, float
                    // bigInt => when the number is big and is used by big websites eg: facebook
                    // string => '' , " "
                    // boolean => true/false
                    // null => empty, standalone value
                    // undefined => variable created but value is not define.
                    // symbol => unique-ness k liye use hota h

// object

// to know the type of any entered value
console.log(typeof " himanshu");  //string type value

let age = 20
console.log(typeof age);// number type value

console.log(typeof null);   // object  >>>> null is a object
console.log(typeof undefined);   // undefined


                